{% extends 'base.html.twig' %}

{% block body %}
    <h1>Chatusuariousuario</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chatUsuarioUsuario.id }}</td>
            </tr>
            <tr>
                <th>Mensaje</th>
                <td>{{ chatUsuarioUsuario.mensaje }}</td>
            </tr>
            <tr>
                <th>Createdat</th>
                <td>{{ chatUsuarioUsuario.createdAt }}</td>
            </tr>
            <tr>
                <th>Visto</th>
                <td>{% if chatUsuarioUsuario.visto %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('chatusuariousuario_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('chatusuariousuario_edit', { 'id': chatUsuarioUsuario.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}

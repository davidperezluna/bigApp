{% extends 'base.html.twig' %}

{% block body %}
    <h1>Productopromocion</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ productoPromocion.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('productopromocion_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('productopromocion_edit', { 'id': productoPromocion.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
